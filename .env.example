# Frontend (Vite) — copy to frontend/.env for local overrides
# Leave empty to use the Vite dev proxy to the API (same origin).
VITE_API_URL=

# Backend
PORT=8080
ENV=development
DB_URL=
CORS_ORIGINS=http://localhost:5173
